import { Story } from "@storybook/react";
import { GroupProducts } from "./GroupProducts";
export default {
  title: "Molecules/GroupProducts",
  component: GroupProducts,
};

const Template: Story = () => <GroupProducts />;
export const Default = Template.bind({});
